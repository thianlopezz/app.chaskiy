import { MonedaDirective } from './moneda.directive';

describe('MonedaDirective', () => {
  it('should create an instance', () => {
    const directive = new MonedaDirective();
    expect(directive).toBeTruthy();
  });
});
